{
  "name": "canton-event-market",
  "version": "0.1.0",
  "private": true,
  "description": "SignalDesk - institutional prediction markets on Canton",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "npm run dev -w @signaldesk/api",
    "dev:web": "npm run dev -w @signaldesk/web",
    "build": "npm run build -ws --if-present",
    "lint": "npm run lint -ws --if-present"
  }
}
